(function(D){
'use strict';
const starterCards=()=>[{id:D.uid(),scene:'coast',title:'A quieter horizon',note:'Teal water, warm sand, and room to breathe.',wide:true},{id:D.uid(),scene:'arches',title:'Soft structure',note:'Warm surfaces. A repeating rhythm.'},{id:D.uid(),scene:'orbital',title:'A touch of the unexpected',note:'One luminous, deliberate accent.'}];
D.register({id:'gemini',name:'Gemini',rank:10,pattern:'Brief + visual board',description:'Explore an idea in conversation while a visual board takes shape. Rearrange, annotate, and collect without losing the brief.',tip:'Try “Create a mountain film storyboard”. Edit a card, move it, and export the resulting board as Markdown or JSON.',boundary:'Board generation is a deterministic local selection of original assets and templates. No Google Gemini model, image generation, or cloud service is connected.'},ctx=>{
 const {root,scope}=ctx;const seed={messages:[],prompt:'',context:'',title:'A slower kind of beautiful',description:'A visual direction for quiet days and thoughtful places.',cards:starterCards()};const m=()=>ctx.get(seed);
 function render(){const s=m();root.innerHTML=D.appHeader('gemini','Offline storyboard engine',D.btn('new-board','plus','New board','small ghost'))+D.paneTabs('Creative brief','Moodboard')+`<div class="duo-panes"><div class="pane primary-pane"><div class="scroll" id="gemini-scroll">${!s.messages.length?`<div class="chat-intro gemini-intro"><span class="intro-mark">${D.brand('gemini',41)}</span><h2>Hello, possibility.</h2><p>A little inspiration on one side.<br>A whole new direction on the other.</p></div><div class="prompt-grid">${[['image','Create a coastal brand direction'],['video','Storyboard a mountain film'],['palette','Explore warm architecture'],['spark','Make a future-facing moodboard']].map(([i,t])=>`<button class="prompt-card" data-action="suggest" data-prompt="${t}">${D.icon(i)}<span>${t}</span></button>`).join('')}</div><div class="padded"><div class="note-box">${D.icon('spark',15)} This local studio selects from original digital studies. Every card is editable. There is no model connection.</div></div>`:''}<div class="chat-messages">${s.messages.map(msg=>`<div class="chat-message ${msg.role}">${msg.role==='assistant'?D.brand('gemini',24):''}<div class="message-text">${D.escape(msg.text)}</div></div>`).join('')}</div></div><form class="composer" id="gemini-form">${s.context?`<div class="context-card">${D.icon('file',16)}<div><b>Reference attached</b><p>${D.escape(s.context.slice(0,170))}</p></div></div>`:''}<div class="composer-box">${D.icon('spark',19)}<textarea name="prompt" rows="2" placeholder="Give your next idea some space…" aria-label="Creative brief">${D.escape(s.prompt)}</textarea><button class="send-btn" type="submit" aria-label="Build board from brief">${D.icon('arrow')}</button></div><div class="composer-hint">Local templates & original assets · no remote model</div></form></div><div class="pane secondary-pane"><div class="pane-toolbar"><div><h3>Creative canvas</h3><small>${s.cards.length} cards · autosaved</small></div><div class="row" style="gap:3px">${D.ib('edit-board','edit','Edit board title')}${D.ib('add-card','plus','Add a card')}${D.ib('share-board','share','Share board')}${D.ib('export-board','download','Export board')}</div></div><div class="board"><div><span class="eyebrow muted">VISUAL DIRECTION</span><h2 class="board-title" style="margin-top:9px">${D.escape(s.title)}</h2><p class="board-description" style="margin-top:7px">${D.escape(s.description)}</p></div><div class="board-grid">${s.cards.map((card,i)=>{const scene=D.scenes.find(x=>x.id===card.scene)||D.scenes[0];return `<article class="board-card ${card.wide?'wide':''}" draggable="true" data-card-id="${card.id}">${D.image(scene.id,scene.name)}<div class="board-card-content"><h3>${D.escape(card.title)}</h3><p>${D.escape(card.note)}</p><div class="palette-swatches">${scene.palette.map(c=>`<button style="background:${c}" data-action="copy-color" data-color="${c}" title="Copy ${c}" aria-label="Copy color ${c}"></button>`).join('')}</div><div class="card-actions"><span class="tiny muted" style="font-size:8px">${String(i+1).padStart(2,'0')} · ${D.escape(scene.kind)}</span><div class="row" style="gap:0">${D.ib('card-up','up','Move card up',`data-index="${i}" ${i===0?'disabled':''}`)}${D.ib('card-edit','edit','Edit card',`data-index="${i}"`)}${D.ib('card-remove','close','Remove card',`data-index="${i}"`)}</div></div></div></article>`;}).join('')}</div><div class="note-box">Drag cards to reorder, or use the arrow buttons. Tap a color to copy its value. On compact screens, switch back to your brief with the tabs above.</div></div></div></div>`;ctx.pane(root.dataset.activePane||'primary');const scroll=D.$('#gemini-scroll',root);if(s.messages.length)scroll.scrollTop=scroll.scrollHeight;}
 function build(text){text=(text??m().prompt).trim();if(!text)return;D.store.checkpoint('gemini');const s=m(),t=text.toLowerCase();let scenes=['coast','dunes','arches'];if(/mountain|alpine|film/.test(t))scenes=['alpine','dunes','golden'];else if(/architecture|warm|interior/.test(t))scenes=['arches','golden','orbital'];else if(/future|space|technology/.test(t))scenes=['orbital','alpine','coast'];s.title=/film/.test(t)?'A film with room to breathe':/brand/.test(t)?'The art of slowing down':text.slice(0,65);s.description=text.slice(0,180)+(s.context?' · Includes your shared reference.':'');s.cards=scenes.map((id,i)=>{const sc=D.scenes.find(s=>s.id===id);return {id:D.uid(),scene:id,title:/film/.test(t)?['01 · Establish the world','02 · Find the rhythm','03 · Leave an impression'][i]:sc.name,note:sc.text,wide:i===0};});s.messages.push({role:'user',text},{role:'assistant',text:'I’ve assembled three complementary studies on your canvas using the local collection. Change the titles and notes, compare their palettes, and move the cards until the direction feels right.'});s.messages=s.messages.slice(-40);s.prompt='';ctx.save();render();}
 function editCard(index){const card=m().cards[index];if(!card)return;D.dialog('Shape this card',`<div class="stack"><label class="field-label">Title<input class="field" name="title" value="${D.escape(card.title)}" maxlength="100"></label><label class="field-label">Note<textarea class="field" name="note" rows="4" maxlength="2000">${D.escape(card.note)}</textarea></label><label class="field-label">Original study<select name="scene" class="field">${D.scenes.map(s=>`<option value="${s.id}" ${card.scene===s.id?'selected':''}>${s.name}</option>`).join('')}</select></label><label class="row tiny"><input type="checkbox" name="wide" ${card.wide?'checked':''}> Span both board columns</label></div><div class="dialog-footer"><span class="tiny muted">Changes stay on this device</span><button class="btn primary" id="save-card">Save card</button></div>`,body=>{D.$('#save-card',body).onclick=()=>{D.store.checkpoint('gemini');Object.assign(card,{title:D.$('[name=title]',body).value,note:D.$('[name=note]',body).value,scene:D.$('[name=scene]',body).value,wide:D.$('[name=wide]',body).checked});ctx.save();D.$('#system-dialog').close();render();};});}
 function markdown(){const s=m();return `# ${s.title}\n\n${s.description}\n\n`+s.cards.map((c,i)=>{const sc=D.scenes.find(s=>s.id===c.scene);return `## ${i+1}. ${c.title}\n\n${c.note}\n\nOriginal study: ${sc?.name}\nPalette: ${sc?.palette.join(', ')}\n`;}).join('\n');}
 ctx.act('suggest',b=>build(b.dataset.prompt));ctx.act('copy-color',b=>D.copy(b.dataset.color));ctx.act('card-edit',b=>editCard(+b.dataset.index));ctx.act('card-remove',b=>{D.store.checkpoint('gemini');m().cards.splice(+b.dataset.index,1);ctx.save();render();});ctx.act('card-up',b=>{const i=+b.dataset.index;if(i>0){D.store.checkpoint('gemini');[m().cards[i-1],m().cards[i]]=[m().cards[i],m().cards[i-1]];ctx.save();render();}});
 ctx.act('new-board',()=>{D.store.checkpoint('gemini');Object.assign(m(),{messages:[],prompt:'',context:'',title:'An open canvas',description:'A new idea starts here.',cards:[]});ctx.save();render();});
 ctx.act('add-card',()=>{D.store.checkpoint('gemini');m().cards.push({id:D.uid(),scene:D.scenes[m().cards.length%D.scenes.length].id,title:'A new direction',note:'Describe what this adds to your story.',wide:false});ctx.save();render();editCard(m().cards.length-1);});
 ctx.act('edit-board',()=>D.dialog('Name your direction',`<div class="stack"><label class="field-label">Board title<input class="field" name="title" value="${D.escape(m().title)}" maxlength="120"></label><label class="field-label">Description<textarea class="field" name="description" rows="3">${D.escape(m().description)}</textarea></label><button class="btn primary" id="save-title">Save direction</button></div>`,body=>{D.$('#save-title',body).onclick=()=>{D.store.checkpoint('gemini');m().title=D.$('[name=title]',body).value;m().description=D.$('[name=description]',body).value;ctx.save();D.$('#system-dialog').close();render();};}));
 ctx.act('share-board',()=>ctx.share(markdown(),m().title));ctx.act('export-board',()=>D.dialog('Export your board',`<p>Your editable board is available as Markdown for sharing, or JSON for integration.</p><div class="row"><button class="btn primary" id="md">Export Markdown</button><button class="btn ghost" id="json">Export JSON</button></div>`,body=>{D.$('#md',body).onclick=()=>D.download('duo-moodboard.md',markdown(),'text/markdown');D.$('#json',body).onclick=()=>D.download('duo-moodboard.json',JSON.stringify({title:m().title,description:m().description,cards:m().cards},null,2),'application/json');}));
 scope.on(root,'submit',e=>{if(e.target.id==='gemini-form'){e.preventDefault();build();}});scope.on(root,'input',e=>{if(e.target.name==='prompt'){m().prompt=e.target.value;ctx.save();}});scope.on(root,'keydown',e=>{if(e.target.name==='prompt'&&e.key==='Enter'&&!e.shiftKey){e.preventDefault();build();}});
 let dragged=null;scope.on(root,'dragstart',e=>{const card=e.target.closest('[data-card-id]');if(card){dragged=card.dataset.cardId;e.dataTransfer.setData('text/x-duo-card',dragged);e.dataTransfer.effectAllowed='move';}});scope.on(root,'dragover',e=>{if(e.target.closest('[data-card-id]')&&dragged)e.preventDefault();});scope.on(root,'drop',e=>{const card=e.target.closest('[data-card-id]');if(!card||!dragged)return;e.preventDefault();const from=m().cards.findIndex(c=>c.id===dragged),to=m().cards.findIndex(c=>c.id===card.dataset.cardId);if(from>=0&&to>=0){D.store.checkpoint('gemini');m().cards.splice(to,0,m().cards.splice(from,1)[0]);ctx.save();render();}dragged=null;});scope.on(root,'dragend',()=>dragged=null);
 function receive(a){m().context=String(a.text||'').slice(0,5000);m().prompt='Create a visual direction for '+(a.title||'this reference');ctx.save();render();ctx.pane('primary');}scope.cleanup(D.wrapDrop(root,receive));render();return {render,receive};
});
})(window.Duo);
